import { Global, css, jsx } from '@emotion/react';

export const GlobalStyle = () => (
    <Global
        styles={css`
            * {
                //color: hotpink;
            }
        `}
    />
);
